services:
  firefox:
    image: selenium/standalone-firefox:4.43.0-20260404
    shm_size: 2gb
    ports:
      - "4444:4444"
      - "7900:7900"
    # environment:
    #   - SE_VNC_NO_PASSWORD=1            # 關掉 VNC 密碼
    #   - SE_VNC_VIEW_ONLY=1              # 唯讀模式 (只能看不能控)
    #   - SE_NODE_MAX_SESSIONS=2          # 同一容器內可平行跑幾個 session
    #   - SE_NODE_OVERRIDE_MAX_SESSIONS=true
    #   - SE_START_XVFB=false             # 純 --headless 時關掉 Xvfb 省資源
